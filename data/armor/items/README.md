# Armor Item Definitions

Place `ArmorItemData` resources (`.tres` or `.res`) in this folder or subfolders.

Each item can define category, condition, visuals, generic attributes, future effect data, and metadata without changing the core armor system.
